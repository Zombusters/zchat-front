import { connect } from 'react-redux';

import MapPage from '../../components/Main/pages/MapPage.js';

function mapStateToProps(state) {
    return {
    };
}

export default connect(mapStateToProps, {})(MapPage);
